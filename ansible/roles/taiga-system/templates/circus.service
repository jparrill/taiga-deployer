[Unit]
Description=Taiga server
After=syslog.target
After=network.target

[Service]
Type=simple
User=taiga
Group=taiga
ExecStart=/bin/circusd --daemon {{ taiga_path }}/circus.ini
TimeoutSec=300

[Install]
WantedBy=multi-user.target
